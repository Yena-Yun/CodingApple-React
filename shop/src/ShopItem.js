import React from "react";
import styled from "styled-components";

const ShopItem = (props) => {
  return <ShopItemBlock></ShopItemBlock>;
};

const ShopItemBlock = styled.div``;

export default ShopItem;
